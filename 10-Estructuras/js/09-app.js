const autenticado = true;
const puedePagar = false;
const saldo = true;

console.log( autenticado ? puedePagar ? 'Si esta autenticado y puede pagar' : 'Si autenticado pero, no puede pagar' : 'No está autenticado');
