const producto = 'Monitor de 29 pulgadas';

console.log(producto);
console.log(producto.length);

//Conocer la cantidad de letras de cadenas de texto
console.log([producto.indexOf('pulgadas')]);

//Buscar la coincidencia en una cadena de texto
console.log([producto.includes('pulgadas')]);