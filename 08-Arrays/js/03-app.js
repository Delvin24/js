const meses = ['enero', 'febrero', 'marzo', 'abril', 'mayo', 'agosto'];
console.table(meses);

//Debemos saber cuanto mide el arreglo

//Recorriendo el arreglo
for(let i = 0; i < meses.length; i++) {
    console.log( meses [i] );
}